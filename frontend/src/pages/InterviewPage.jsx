/**
 * InterviewPage.jsx
 * Entry point for /practice-interview route.
 * Replaced by the immersive InterviewSimulator — this file now just re-exports it.
 *
 * ASSUMPTION: App.jsx imports InterviewPage from this path. No routing change needed.
 */
export { default } from "./interview/InterviewSimulator";